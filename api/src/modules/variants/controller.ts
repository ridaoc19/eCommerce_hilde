import { Router } from 'express';
// import variantServices from './services';
// const { createVariant, updateVariant, deleteVariant, getVariant } = variantServices;

const router = Router();

// router.post('/create/:product_id', createVariant);
// router.put('/edit/:variant_id', updateVariant);
// router.delete('/delete/:variant_id', deleteVariant);
// router.get('/request/:variant_id', getVariant);

export { router };
